# services/admin 환경변수 예시
#
# M1 (부트스트랩) 시점에는 admin 전용 env 가 없다.
# M2 (로그인 + ADMIN role) 진입 시 services/web 의 web.env.example 패턴을 미러하여
# VITE_API_BASE_URL 등 빌드 타임 변수를 본 파일에 추가한다.
#
# 운영 NAS 의 admin.prod.env 는 /volume3/docker/terab/admin.prod.env 에 별도로 둔다.
