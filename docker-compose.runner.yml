# GitHub Actions Self-hosted Runner
# 사용법: NAS SSH 접속 후 아래 명령 실행
#
#   cp application-runner.properties.example application-runner.properties  # 최초 1회
#   vi application-runner.properties                                         # ACCESS_TOKEN 값 입력 후 저장
#   docker compose -f docker-compose.runner.yml up -d
#
# ACCESS_TOKEN 발급:
#   GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
#   → Generate new token (classic) → scope: repo 체크
#
# 컨테이너 재시작 시 ACCESS_TOKEN으로 RUNNER_TOKEN을 자동 갱신함 (수동 갱신 불필요)

services:
  github-runner:
    image: myoung34/github-runner:latest
    container_name: github-runner
    restart: always
    env_file:
      - runner.env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner_data:/home/runner

volumes:
  runner_data:
