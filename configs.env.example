# configs.env.example — 운영 Docker Config 키 목록
# NAS에서 configs.env로 복사 후 값 채움 → make setup
db_url=
db_name=
db_user=
minio_endpoint=
minio_root_user=
minio_bucket=
owner_username=
owner_nickname=
jwt_access_expiry_ms=
jwt_refresh_expiry_ms=
cors_allowed_origins=
